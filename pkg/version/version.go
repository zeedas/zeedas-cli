package version

// nolint:gochecknoglobals
var (
	// Arch specifies the CPU architecture of the zeedas-cli build.
	Arch = "unset"
	// BuildDate states the date of the zeedas-cli build.
	BuildDate = "unset"
	// Commit states the commit of the zeedas-cli build.
	Commit = "unset"
	// OS specifies the target operating system of the zeedas-cli build.
	OS = "unset"
	// Version states the version of the zeedas-cli build.
	Version = "unset"
)
